{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp-config.schema.json",
  "$comment": "MCP servers required for the work plugin. These should be configured in your Claude Code settings.",
  "mcpServers": {
    "linear": {
      "$comment": "Required: Linear MCP for issue management (HTTP streamable)",
      "type": "http",
      "url": "https://mcp.linear.app/mcp",
      "headers": {
        "Authorization": "Bearer ${LINEAR_API_KEY}"
      }
    },
    "context7": {
      "$comment": "Optional: Context7 MCP for documentation lookup (HTTP streamable)",
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      }
    },
    "sentry": {
      "$comment": "Optional: Sentry MCP for error tracking (HTTP streamable)",
      "type": "http",
      "url": "https://mcp.sentry.dev/mcp"
    },
    "serena": {
      "$comment": "Required: Serena MCP for semantic code operations",
      "type": "stdio",
      "command": "serena",
      "args": ["start-mcp-server", "--context=claude-code", "--project-from-cwd"]
    }
  }
}
